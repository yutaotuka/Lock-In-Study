<div class="main-content">
  <%= image_tag "drone.png", width:430, height:240 %>
    <h1>あなたが集中して勉強出来るように<br>サポートします</h1>
    <% if logged_in? %>
      <%= button_to '使ってみる', study_records_path, method: :get, class: 'try-button' %>
    <% else %>
      <%= button_to '使ってみる', guest_login_path, method: :post, class: 'try-button' %>
    <% end %>
    <div class="explanation">
      <div class="line-bot-qr">
        <p>このQRコードを読み込んだらLINEでの通知もできます。</p>
        <%= image_tag "line-bot-qr.png" %>
      </div>
    </div>
    <% if logged_in? %>
      <%= button_to '使ってみる', study_records_path, method: :get, class: 'try-button' %>
    <% else %>
      <%= button_to '使ってみる', guest_login_path, method: :post, class: 'try-button' %>
    <% end %>
</div>